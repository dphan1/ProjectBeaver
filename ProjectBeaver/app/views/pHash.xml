<function name="ph_image_dist_with_hash" if="HAVE_IMAGE_HASH">
   <proto>float ph_image_dist_with_hash(string h1, string h2)</proto>
   <summary>pHash image distance Dat's version.</summary>
   <description>
        Calculate distance between two images.
   </description>
   <code>
<![CDATA[
       ulong64 hash1 = (ulong64) h1;
       ulong64 hash2 = (ulong64) h2; 
       int dist = ph_hamming_distance(hash1, hash2);
       RETURN_DOUBLE(dist);  
            
]]>
</code>
  </function>

<function name="ph_image_dist_test" if="HAVE_IMAGE_HASH">
   <proto>float ph_image_dist_test(string h1, string h2)</proto>
   <summary>pHash image distance Dat's version.</summary>
   <description>
        Calculate distance between two images.
   </description>
   <code>
<![CDATA[
       ulong64 hash1 = std::strtoul(h1, NULL, 0);
       ulong64 hash2 = std::strtoul(h2, NULL, 0);
       int dist = ph_hamming_distance(hash1, hash2);
       RETURN_DOUBLE(dist);  
            
]]>
</code>
  </function>
